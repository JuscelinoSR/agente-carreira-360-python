# Contexto do Projeto

Este é um MVP em Python do Agente Carreira 360.

O projeto lê arquivos JSON da pasta `data/` e gera arquivos Markdown em `outputs/`.

A versão inicial não usa bibliotecas externas e não integra redes sociais.
